const EN = {
    error: {
        permissions: "Insufficient permissions"
    }
}

const ES = {
    error: {
        permissions: "Permisos insuficientes"
    }
}

const locales = {
    EN,
    ES
}

export default locales;
export { EN, ES };