const EN = {
    editProfile: {
        wrongPassword: "Incorrect password"
    }
}

const ES = {
    editProfile: {
        wrongPassword: "Contraseña incorrecta"
    }
}

const locales = {
    EN,
    ES
}

export default locales;
export { EN, ES };